cmake_minimum_required(VERSION 2.8.3)
project(multi_point_nav)

find_package(catkin REQUIRED COMPONENTS rospy actionlib actionlib_msgs geometry_msgs move_base_msgs)

catkin_package()

include_directories()